# SRE-Application-springboot

to run the spring boot app you need 

mvn and java after insatllation :

mvn clean package

after the abv step buid the docker image

docker build -t spring-app .# sre-app-springboot
